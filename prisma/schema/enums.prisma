enum Role {
    USER
    ADMIN
}

enum Pricing {
    FREE
    PREMIUM
}

enum ReviewStatus {
    PENDING
    APPROVED
    REJECTED
}

enum SubscriptionType {
    MONTHLY
    YEARLY
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum ActivityAction {
    FOLLOW
    UNFOLLOW
    WATCHLIST_ADD
    WATCHLIST_REMOVE
    REVIEW_CREATE
    DIARY_LOG
    LIKE_REVIEW
}
