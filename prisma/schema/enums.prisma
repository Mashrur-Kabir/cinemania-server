enum Role {
    USER
    ADMIN
}

enum Pricing {
    FREE
    PREMIUM
}

enum ReviewStatus {
    PENDING
    APPROVED
    REJECTED
}

enum SubscriptionType {
    BASIC // e.g., $9.99/mo
    PRO // e.g., $19.99/mo
    PREMIUM // e.g., $29.99/mo
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    CANCELLED
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum ActivityAction {
    FOLLOW
    UNFOLLOW
    WATCHLIST_ADD
    WATCHLIST_REMOVE
    REVIEW_CREATE
    DIARY_LOG
    LIKE_REVIEW
}

enum NotificationType {
    // Social Interactions
    FOLLOW
    LIKE_REVIEW
    COMMENT_ADD // When someone comments on your review
    COMMENT_REPLY // Someone replied to your specific comment

    // Moderation (User Alerts)
    REVIEW_APPROVED
    REVIEW_REJECTED

    // Content Alerts (For Users/Admins)
    WATCHED_MEDIA
    REPORT_ALERT // Alert Admins when a review is reported

    // System
    SYSTEM_ANNOUNCEMENT
}
